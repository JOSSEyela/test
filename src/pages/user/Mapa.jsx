// El mapa dedicado fue eliminado.
// La vista de mapa vive dentro del landing page (MapSection).
import { Navigate } from 'react-router-dom';

export default function Mapa() {
  return <Navigate to="/" replace />;
}
